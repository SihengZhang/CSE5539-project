# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new Python project repository. The project structure and architecture are not yet established.

## Setup

Based on the `.gitignore`, this project uses Python. Standard Python project setup will likely include:
- Creating a virtual environment: `python -m venv .venv`
- Activating the environment: `source .venv/bin/activate` (macOS/Linux) or `.venv\Scripts\activate` (Windows)
- Installing dependencies: `pip install -r requirements.txt` (once created)

## Development

Project-specific build, test, and run commands will be added as the codebase develops.
